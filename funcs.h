#pragma once
#include <string>
#include <cctype>
#include <fstream>
// add prototypes here

std::string removeLeadingSpaces(std::string);
int countChar(std::string, char);
std::string indent(std::string);
